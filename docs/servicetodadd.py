# this is the type of thing id like for a service

# create something for buttons
button = get_button(somewaytogetthebutton)
if not button:
  assert that there is no button to use


def button.create_button(self):
  self.button_id = button.create_new_button_id
  button.color = self.button_color
  button.text = self.button_text
  button.action = somehow run a function with this button
  button.locked_status = self.button_locked_status
  button.attached_message = idk how to do this
  button.cooldown = self.button_cooldown # we need to do this somehow
  button.default_error_message = get_default_error_message
  button.allowed_users = # idk how to do this but make it optinal
  button.processing_request: bool
  button.is_link == whether the button has a set url
  button.url = self.button_url

  def EmbedService.createEmbed(fjafhdsjkfhldaskjf)
  
  
